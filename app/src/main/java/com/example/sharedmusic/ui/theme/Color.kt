package com.example.sharedmusic.ui.theme

import androidx.compose.ui.graphics.Color

// ─────────────────────────────────────────────────────────────
// Палитра SharedMusic: «Dark Black + Pink»
// Используется в Theme.kt и в RecordDisc.
// ─────────────────────────────────────────────────────────────

// Брендовый розовый
val PinkPrimary = Color(0xFFFF4D8D)
val PinkOnPrimary = Color(0xFF2E0014)
val PinkPrimaryContainer = Color(0xFF55182F)
val PinkOnPrimaryContainer = Color(0xFFFFD9E5)

// Вторичный (приглушённый розовый)
val PinkSecondary = Color(0xFFCF8DB0)
val PinkSecondaryContainer = Color(0xFF3A2530)
val PinkOnSecondaryContainer = Color(0xFFFFD9E5)

// Чёрные
val BlackBackground = Color(0xFF0B0B0F)
val BlackSurface = Color(0xFF121217)
val BlackSurfaceVariant = Color(0xFF23232B)
val BlackSurfaceContainer = Color(0xFF1A1A21)

// Текст
val OnSurfaceLight = Color(0xFFF6E9EF)
val OnSurfaceVariantMuted = Color(0xFFB9A8B0)

// Ошибка (тёплый светлый — чтобы не конфликтовал с розовым)
val ErrorLight = Color(0xFFFFB4AB)